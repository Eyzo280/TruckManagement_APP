import 'package:flutter/material.dart';

class ChiefLookTrucker extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kierowcy'),
      ),
      body: Center(
        child: Text('Lista'),
      ),
    );
  }
}